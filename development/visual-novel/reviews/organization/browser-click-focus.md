# Browser button focus — September 9, 2026

This records the first semantic-focus repair. A later closing-theme case required bounded pointer recovery and pause-state synchronization; see [the subsequent diagnosis and verified closing sequence](browser-closing-pointer.md). The hashes below identify the earlier eight-cycle test, not the final helper revision.

The bounded check passed eight opening dialogue lines, with 40 real mouse clicks: open Glossary, Return, open People, select Cali, Return on each line. This includes the First Breath/Sanctuary reveal at `opening_003`. Every menu visit preserved the current dialogue. All 40 focus checks matched, with no recorded JavaScript or game errors. The complete 32-scene run remains a separate check.

The actual Chromium probe showed `SayBehavior` focused before pointer movement. Moving to the Glossary button's engine rectangle `[1119, 1026, 121, 54]` produced a focused `Button` whose text was exactly `Glossary`, which belonged to the current `quick_menu` screen. Both transition flags were false. The initial-line probe retained a stable widget ID; it did **not** itself reproduce object replacement. The earlier failure's object-ID wait was nevertheless tied to an instance captured before hover, although screen reevaluation may replace that instance.

Only `click_button` in [browser_smoke.py](../../../../visual-novel/scripts/browser_smoke.py) was changed here. It now checks the current focused button's text, membership in the current requested screen and completed transitions together. It then sends a real mouse click. It preserves the existing visible-button and subsequent screen/content assertions, and reports the last observed label/membership if focus times out. It does not call button actions directly or retry clicks blindly.

The executable [bounded check](browser-click-focused-check.py) extracts the actual helper from the production smoke script. Its [receipt](browser-click-focused-check.json) records all dialogue lines, clicks, focus results, source/helper/runner SHA-256 hashes, and screenshot hashes; the [log](browser-click-focused-check.log) records each completed cycle. Tested at `http://127.0.0.1:8000/?preview=0.1-alpha` in Chromium 148.0.7778.96 at 1280 × 720, completed at 13:29:45 UTC. Helper SHA-256: `d50dad693ccdabdca83d8a776dda8a8be3662cdb61d684c43e930b5f1658e97d`.

The [focus probe](browser-click-focus-diagnose.py), [before/after focus observations](browser-click-focus-diagnosis.json) and [hover capture](browser-click-focus-diagnosis.png) preserve the diagnosis. The final [Glossary](browser-click-glossary.png), [People](browser-click-people.png) and [returned opening dialogue](browser-click-returned.png) captures were visually inspected and show the intended screens. This bounded test does not establish the cause or resolution of the earlier scene-15 miss by itself; the full run must verify that later path.
