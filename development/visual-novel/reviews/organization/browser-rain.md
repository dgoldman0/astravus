# Browser rain handoff — September 9, 2026

Historical pre-fix diagnosis. The [subsequent repair](../audio/browser-handoff/README.md) confirms the pending-loop cause and passes the complete browser check on the rebuilt web package. The observations below retain their original scope.

The bounded probe reproduced the failed ambience handoff. After entering **Mural remembrance** through the real chapter menu and advancing normally with Space into **Treehouse remembrance**, both `renpy.music.get_playing(channel="ambience")` and the backend playing name remained `audio/garden_air.ogg` for ten seconds. Rain was present in the queue and loop list throughout. **The full browser smoke test has not passed; this remains an unresolved browser playback limitation.**

At the first treehouse line, garden playback position was 0.106 seconds; at the 10.005-second observation it was 10.111 seconds. Backend queue depth remained 2. The ambience channel was playing, unpaused, unmuted and not force-stopped; volume was nonzero. The scene, rainy background and first dialogue line stayed unchanged, and no transition was active. There were no recorded JavaScript errors or failed requests; Chromium logged software-WebGL performance warnings. The [cue capture](browser-rain-at-cue.png) and [ten-second capture](browser-rain-after-10s.png) were visually inspected and are byte-identical. This is more than an early scene-state read, but the probe does not establish how often ordinary reading reproduces the problem.

The game registers ambience with `loop=True`. Mural requests garden air; treehouse requests rain with a two-second fadeout, three-second fadein and `if_changed`. Those source instructions agree with the expected rain check. In the pinned Ren'Py 8.5.3 Web Audio implementation, `fadeout()` has an early-return branch when current playback has not started: it promotes queued playback and calls `start_playing()` without applying the requested fadeout. A decode/start race during rapid scene advancement is therefore a **source-based, unconfirmed hypothesis**, not an established root cause. No game/audio or smoke-test change was made for this probe, and the expected rain criterion was not weakened.

The completed [runner](browser-rain-probe.py), [state receipt](browser-rain-probe.json) and [log](browser-rain-probe.log) preserve all observations, actual dialogue steps, source/audio/SDK hashes and screenshot hashes. Chromium 148.0.7778.96, 1280 × 720, `http://127.0.0.1:8000/?preview=0.1-alpha`; completed 13:56:47 UTC. The receipt SHA-256 is `51eb97f454d953b85db1a5ff063efee5ea5ce95d9fad73e263beb5c19eb765e2`. Its `delay_to_10s` field is the SDK's time until playback position ten seconds, not a measured fade/decode delay.

An optional [instrumentation runner](browser-rain-trace.py) ended before a second scene probe because `renpyAudio` was unavailable in the page JavaScript context; its [error log](browser-rain-trace.log) is retained and supplies no additional transition evidence. Investigation stopped at the coordinator's request. The inspected hypothesis source, `visual-novel/.cache/renpy-8.5.3-sdk/renpy/common/_audio.js`, has SHA-256 `b40467d06b40f98e3cb83abd3b840db730e6bb8275d49fba22fc06de38e51879`. No conclusion about the packaged native build follows from this browser-only failure.
